export const createAllianceLogoURL = (
    shape: number,
    pattern: number,
    icon: number,
): string => {

    return (`https://cdn.galaxylifegame.net/content/img/alliance_flag/AllianceLogos/flag_${shape}_${pattern}_${icon}.png`)
}
