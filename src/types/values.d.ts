// Estilo para componentes UI
type UIStyle = 'primary' | 'secondary' | 'danger' | 'success';

// Estilo de estatus en componentes
type StatusStyle = 'success' | 'warning' | 'error';

type InputType = "text" | "search" | "number" | "password";

type InputAutoCapitalizeType = "characters" | "none" | "off" | "on" | "sentences" | "words"

// Comportamiento de selección de opciones
type SelectableOptionsBehavior = 'switch' | 'alwaysActive' | 'multiOption';
